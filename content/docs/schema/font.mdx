---
title: Font
description: Documentation for the Font schema definition in the Polotno Design JSON Schema.
keywords: ["Font", "JSON Schema", "Polotno", "Design", "API", "Documentation"]
---

# Font



## Type Information

**Base Type:** `object`



## Properties


| Property | Type | Description |
|----------|------|----------|
| `fontFamily` | `string` | Font family name. |
| `styles` | Array\<[`FontStyleVariant`](./font-style-variant)\> | Font style variants for multi-weight/style font families. Each entry generates a separate |
| `url` | `string` | URL to load the font from. Used when the font has a single style variant. |



## JSON Schema

```json
{
  "additionalProperties": false,
  "properties": {
    "fontFamily": {
      "description": "Font family name.",
      "type": "string"
    },
    "styles": {
      "description": "Font style variants for multi-weight/style font families. Each entry generates a separate",
      "items": {
        "$ref": "#/definitions/FontStyleVariant"
      },
      "type": "array"
    },
    "url": {
      "description": "URL to load the font from. Used when the font has a single style variant.",
      "type": "string"
    }
  },
  "required": [
    "fontFamily"
  ],
  "type": "object"
}
```

## Related Definitions

- [FontStyleVariant](./font-style-variant)
