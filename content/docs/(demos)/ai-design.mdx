---
title: AI Design Generation
description: Generate designs using AI with a custom side panel for prompt input
---

<Callout type="warn">
  This feature is experimental.
</Callout>

Use the Polotno AI API to generate complete designs from text prompts. This demo shows how to build a custom side panel that integrates with the AI design generation endpoint.

## Features

- **Text prompt input** — describe the design you want
- **JSON design output** — the API returns a complete Polotno JSON design

## How it works

1. User enters a design prompt in the custom AI Design side panel
2. The app sends a POST request to the Polotno AI API
3. The API returns a Polotno JSON design which is loaded via `store.loadJSON()`

## API

The demo uses the AI design creation endpoint:

```
POST https://api.polotno.com/api/ai/design/create?KEY=<your-api-key>
```

The API accepts a `prompt` field and returns a JSON design:

Request body:

```json
{
  "prompt": "A business card with blue gradient"
}
```

Response: a Polotno-compatible JSON design object that can be loaded directly with `store.loadJSON()`.

<Callout type="info">
  Each AI design generation request is billed at **$0.20 per design**
</Callout>

## Live demo

<Sandbox github="polotno-project/polotno-docs/tree/main/examples/polotno-ai-design" />
