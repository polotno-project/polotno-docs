# Polotno examples (auto-generated — do not edit)

This repository is generated automatically. **Do not commit changes here**;
they will be overwritten.

The source of truth is the private `polotno-website` repo under `examples/`.
A GitHub Action mirrors that folder here on every push, so CodeSandbox and
StackBlitz embeds — which can only import from a public repo — always serve
the latest demo code.

Open any example in CodeSandbox:

```
https://codesandbox.io/p/sandbox/github/polotno-project/polotno-docs/tree/main/examples/<example-name>
```
